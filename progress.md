Original prompt: ตอนนี้เริ่มงานจากตัวเก่า ต่อที่ แก้ไขการโจมตีไม่ใช้ทำง่ายๆเช่น tag dummy ฉันไปนั่งไล่มา ทำเป็นค่าเป้าหมายที่โดนในระยะโจมตี

# Progress

## Current Flow
- Active flow: `Main Menu -> Select Character -> Create Character -> Ritual -> Play`
- Legacy pages `setup.html`, `start-run.html`, `briefing.html` are no longer part of the real flow.
- Opening those legacy routes now redirects back into the active flow.

## Current State
- Web game runs locally on `http://localhost:4173`
- Runtime is now fully canvas/requestAnimationFrame based with the current Tailwind/CSS shell
- Project is in `prototype / playable systems` stage, not content-complete

## Next AI Start Here
- เปิดไฟล์ให้น้อยที่สุดก่อน:
  - `js/systems/play.js`: pacing, monster AI, spawn rate, player pressure
  - `js/systems/combat.js` + `js/systems/beam.js`: Sword 12 / Sword 17 / combat visuals
  - `script.js`: scene scale / view distance / shared catalogs
- ห้ามสมมติว่าผู้ใช้ต้องการให้รันเทสต์เอง: ตอนนี้ workflow กลับไปเป็น `แก้โค้ดอย่างเดียว เว้นแต่จะสั่งให้เทสตรง ๆ`
- สถานะมอนล่าสุด:
  - มอนรู้ตำแหน่งผู้เล่นตลอดเวลา
  - เดินเข้าหาแบบง่าย ไม่มี pathfinding
  - หยุดที่ระยะตีแล้วโจมตีผ่าน `applyPlayerDamage()`
  - ความเร็วเดินถูกลดเหลือครึ่งหนึ่งจาก `moveSpeed`
  - ดาเมจมอนถูกกดลงแรงมากแล้ว (`-90%` จากรอบแรกของ AI)
- สถานะภาพรวมล่าสุด:
  - มุมมองในหน้าเล่นถูกซูมออกโดยลด `tileSize` ใน `buildSceneLayout()`
  - Sword 12 + Sword 17 ทำงานพร้อมกันได้ และลำแสงออกจากดาบที่หมุนอยู่จริง
  - Twin Spin ใช้เวอร์ชัน trail ก่อนหน้าแล้ว ผู้ใช้พอใจกว่าแบบ motion-blur รอบล่าสุด
- งานที่ยังตั้งใจให้เรียบง่ายไว้:
  - มอนยังไม่มีอนิเมชันโจมตี
  - มอนยังไม่มี knockback / hitstun / death behavior พิเศษ
  - AI ยังไม่มีระบบแยกฝูงหรือกันการซ้อนตำแหน่ง

## Main Pages
- [index.html](/Users/mon/roguelike-endless-skill/index.html): main menu
- [select.html](/Users/mon/roguelike-endless-skill/select.html): character slot select + delete
- [create.html](/Users/mon/roguelike-endless-skill/create.html): create character
- [ritual.html](/Users/mon/roguelike-endless-skill/ritual.html): choose 3 skills
- [play.html](/Users/mon/roguelike-endless-skill/play.html): playable scene

## Core Systems
- [script.js](/Users/mon/roguelike-endless-skill/script.js): shared state, data catalogs, persistence, ritual data, inventory data
- [js/systems/stats.js](/Users/mon/roguelike-endless-skill/js/systems/stats.js): player stat formulas, derived values, and ratio helpers
- [js/systems/inventory.js](/Users/mon/roguelike-endless-skill/js/systems/inventory.js): inventory state restore/normalize, slot helpers, quick transfer, container helpers
- [js/systems/entities.js](/Users/mon/roguelike-endless-skill/js/systems/entities.js): monster catalog, monster stats, ambient spawn/restore helpers, monster draw helpers
- [js/systems/damage.js](/Users/mon/roguelike-endless-skill/js/systems/damage.js): shared damage formula, mode multipliers, defense mitigation, final damage resolution
- [js/systems/beam.js](/Users/mon/roguelike-endless-skill/js/systems/beam.js): Prism Beam runtime, visuals, hit sweep, Sword 12 beam helpers
- [js/systems/combat.js](/Users/mon/roguelike-endless-skill/js/systems/combat.js): combat logic
- [js/systems/play.js](/Users/mon/roguelike-endless-skill/js/systems/play.js): play scene, HUD, inventory interactions, rendering helpers
- [js/systems/pages.js](/Users/mon/roguelike-endless-skill/js/systems/pages.js): page init + route behavior

## Recent Notes
- 2026-03-20: Removed the special front-body-over-fangs layering in `js/systems/play.js`. Titan Centipede body sections `1-5` are no longer redrawn after the fang pass, so the fang pair now stays above the full body again while the head remains on top.
- 2026-03-20: Moved Titan Centipede fang spacing in `js/systems/play.js` onto live render-time config instead of relying on the stored values from when the boss was first summoned. This means spacing tweaks now show up immediately on the current boss in-scene, which fixes the “changed code but fangs still look unchanged” problem during rapid visual tuning.
- 2026-03-20: Increased the Titan Centipede base fang spacing another 10% in `js/systems/play.js` by raising the baseline `fangOffsetY` from `0.638` to `0.7018`. This specifically widens the closed/default fang position further before any beam-fire opening is layered on top.
- 2026-03-20: Spread the Titan Centipede fang pair slightly farther apart in `js/systems/play.js` by raising the baseline `fangOffsetY` about 10% (`0.58 -> 0.638`). This widens the upper/lower fang gap a bit in both the idle and beam-fire states without changing the hinge logic.
- 2026-03-20: Shortened the Titan Centipede body blueprint in `js/systems/entities.js` from `300 meters` to `150 meters`, keeping `1 segment = 1 meter`. The summon/render path now builds `150 body segments` instead of `300`, which should make boss movement and visual debugging easier to read.
- 2026-03-20: Flipped the Titan Centipede fang-open sign in `js/systems/play.js` so the hinge animation now opens outward instead of folding inward. The upper/lower fang center rotation and draw rotation signs were both swapped together to keep the root planted while the tips separate the correct way.
- 2026-03-20: Corrected the Titan Centipede fang opening pivot in `js/systems/play.js`. The beam-fire animation no longer spreads the fang bases apart; instead each fang now rotates around a hinge point pulled toward the head so the root stays planted and the tip is the part that opens outward.
- 2026-03-20: Wired the Titan Centipede fangs into the boss beam in `js/systems/play.js`. Once the charge completes and the beam actually fires, the fang pair now eases open by widening the upper/lower gap and rotating outward from the head instead of staying locked shut.
- 2026-03-20: Locked the Titan Centipede beam to the boss head pose in `js/systems/play.js`. The head and mouth anchor now resolve from the same authoritative `boss.worldX/worldY/headingAngle` pose during aiming and firing instead of letting the beam read one angle while the head render still followed old trail tangent data.
- 2026-03-20: Added a dedicated Titan Centipede boss beam attack in `js/systems/play.js` for the control test. While `B` control is active, holding `Shift` now charges a multicolor red-blue-gold orb at the boss mouth, reserves mouse input for aiming instead of movement, then fires a matching beam with hit sweep damage; the player combat beam loop is skipped during boss control so the boss attack can own screen shake and beam state cleanly.
- 2026-03-20: Added mouse steering to the Titan Centipede boss-control test in `js/systems/play.js`. While `B` control is active, the boss can now move from cursor direction as well as WASD, using a center-screen deadzone plus distance-scaled mouse vector so the head leads smoothly without drifting when the cursor sits near the middle.
- 2026-03-20: Reworked the Titan Centipede movement path in `js/systems/play.js` so the head now owns the movement direction and the long body is sampled from a live trail of past head positions. During boss-control testing, moving the head with `B` control now drags the 300-segment body behind it instead of rendering the body on a fixed sine strip.
- 2026-03-20: Changed the Titan Centipede boss-control test input in `js/systems/play.js` from `E` to `B`. Pressing `B` near the summoned boss now counts `1/3 -> 2/3 -> 3/3`; on the third press the run switches into `controllingBoss`, camera follows the boss, and movement input drives the boss instead of the player for movement debugging.
- 2026-03-20: Moved the Titan Centipede horn-base alignment fix onto the correct layer in `js/systems/play.js`. The right-side horn base now uses a dedicated `hornBaseAssetOffsetXPx` compensation for the tail-tip sprite's real rotated pixel center, while `hornBaseOffsetX` was reduced so the overall visual placement stays nearly the same instead of baking asset drift into the design offset.
- 2026-03-20: Nudged the Titan Centipede horn base down 1px more in `js/systems/play.js`, raising `hornBasePixelNudgeY` from `5` to `6` for one more micro-alignment pass.
- 2026-03-20: Nudged the Titan Centipede horn base down 2px more in `js/systems/play.js`, raising `hornBasePixelNudgeY` from `3` to `5` for the latest head-alignment tweak.
- 2026-03-20: Nudged the Titan Centipede horn base down 1px more in `js/systems/play.js`, raising `hornBasePixelNudgeY` from `2` to `3` for another small alignment pass under the head.
- 2026-03-20: Moved the Titan Centipede horn base down 4px from the previous pass in `js/systems/play.js`, changing `hornBasePixelNudgeY` from `-2` to `2` so the single tail-tip base sits lower relative to the head during alignment tuning.
- 2026-03-20: Reverted the mirrored horn-base experiment in `js/systems/play.js`. The Titan Centipede now uses a single tail-tip horn base again on the right side, and it gets a small `2px` upward nudge (`hornBasePixelNudgeY`) to line it up more cleanly with the head.
- 2026-03-20: Restored the Titan Centipede horn-base offset in `js/systems/play.js` and made it symmetric with the head. The tail-tip placeholder now renders as a mirrored pair under the head using `hornBaseOffsetX` on both sides of the head centerline instead of a single centered part.
- 2026-03-20: Reverted the extra rightward offset on the Titan Centipede horn-base placeholder in `js/systems/play.js`. `hornBaseOffsetX` is now back to `0`, so the tail-tip horn base sits directly under the head instead of being pushed right for the earlier 50%-shift experiment.
- 2026-03-20: Added a separate Titan Centipede horn-base placeholder in `js/systems/play.js` using the tail-tip frame. It now renders below the head layer, offset to the right of the head (`hornBaseOffsetX`), so future horn construction can build from a dedicated part without disturbing the real tail at the end of the body.
- 2026-03-20: Adjusted Titan Centipede layering in `js/systems/play.js` so body sections `1-5` now render above the fang layer while the head still stays on top. Segments `6+` keep the older lower layer, which gives the front body overlap the user wanted without flattening the whole centipede stack.
- 2026-03-20: Pushed the Titan Centipede fang pair farther left again in `js/systems/play.js` by raising `fangOffsetX` to `scene.tileSize * 1.8` for a much more pronounced forward placement during visual tuning.
- 2026-03-20: Reverted the last upward fang nudge in `js/systems/play.js` and shifted the Titan Centipede fang pair slightly farther left instead by increasing `fangOffsetX` from `scene.tileSize * 0.9` to `scene.tileSize * 1.08`. Vertical alignment is back to the previous baseline.
- 2026-03-20: Nudged the Titan Centipede fang assembly slightly upward in `js/systems/play.js` by adding a separate `fangAnchorYOffset` and moving the whole fang pair up before applying the upper/lower split. Fang size and spread were left unchanged in this pass.
- 2026-03-20: Adjusted Titan Centipede draw layering in `js/systems/play.js` so both fang attachments now render below the head consistently. The order is now head shadow, then fangs, then head, which keeps overlaps readable without changing fang placement or size.
- 2026-03-20: Increased the Titan Centipede test fang render size 5x in `js/systems/play.js` by scaling `fangSize` from `scene.tileSize * 1.8` up to `scene.tileSize * 9`. Positioning was left untouched for this pass so the user can judge pure size first.
- 2026-03-20: Rotated the current Titan Centipede test render 90 degrees into a unified left-facing orientation in `js/systems/play.js`. Head, repeated body, tail, and both fang attachments now all use the same fixed quarter-turn rotation instead of drawing raw sheet orientation directly.
- 2026-03-20: Built the first real in-scene Titan Centipede test body in `js/systems/play.js`. Touching the red trigger now summons an actual large boss render using the separated boss sheets (`head phase`, repeated `body` segments, `tail`, and both `fangs`), with the current test setup drawing a 300-segment body trail from the head. Monster spawning is also paused by default for cleaner boss debugging.
- 2026-03-20: Added a blue world toggle circle to the left of spawn in `js/systems/play.js`. Touching it flips `state.game.monsterSpawnsPaused` on/off, clears active monsters when pausing, and lets the user temporarily re-enable ambient spawn pressure only when needed during boss tests.
- 2026-03-20: Added a first Titan Centipede boss blueprint in `js/systems/entities.js`. The main sheet is now interpreted as `head phase 1/2/3 + body segment + tail`, fang sheets stay separate, and the body length is locked to `300 meters` using `1 segment = 1 meter` (`300 body segments` for now). `state.game.bossSummonTest` in `js/systems/play.js` now carries that pending boss profile for the summon-test flow.
- 2026-03-20: The red right-side trigger in `js/systems/play.js` is now explicitly a boss-summon test trigger. Runtime state was split into `state.game.bossSummonTrigger` for the world circle and `state.game.bossSummonTest` for the mechanic flags; touching the circle now marks `armed + summonRequested` and shows `Boss summon test armed`, so the next pass can plug real boss spawn logic into that state directly.
- 2026-03-20: Added a simple red world trigger circle in `js/systems/play.js`, placed to the right of the spawn/start-zone origin. The trigger now renders as a pulsing red ring in the play scene, tracks its own `activated` state in `state.game.bossSummonTrigger`, and flips that condition the moment the player touches the circle.
- 2026-03-20: Sword 12 in utility slots no longer pays the generic flying-sword attack drain. `buildUtilityWeaponAttackProfile()` in `js/systems/combat.js` now forces `manaCostPerSecond = 0` when the weapon resolves a native Prism Beam profile, so Sword 12 only spends its beam mana while channeling instead of also losing the extra `15 mana/sec` attack-phase tax.
- 2026-03-20: Reduced live monster chase speed another 50% in `js/systems/play.js` by lowering `MONSTER_MOVE_SPEED_SCALE` from `0.5` to `0.25`, so current monsters advance at half of the already-slowed runtime pace and should apply less rush pressure on the player.
- 2026-03-20: Monster chase in `js/systems/play.js` now biases into a light flank spread near the player instead of always collapsing straight into center point, and each monster gets a short per-entity contact grace the first time it enters melee range so touch-range damage lands less abruptly and stacked swarms feel fairer without adding full pathfinding/telegraph systems.
- 2026-03-19: Normal melee now separates `visualRange` from real hit `range`; swords still render at the same visual reach, while melee hit distance is extended via `weapon.melee.hitRangeScale` so attack coverage matches the visible blade more closely. The shared melee sword default is now tuned to `1.35`.
- 2026-03-19: Normal swing visuals now resolve live melee `range` and `visualRange` from the currently equipped swing weapons every frame, so slash effects follow attack-distance changes in real time instead of staying on the trigger-time snapshot.
- 2026-03-19: The rebuilt normal-swing slash visual was slimmed down again into a shorter, cleaner white air-cut trail along the attack-range edge; the oversized tip flash was removed so the effect reads less like a floating crescent and more like a tight blade slice.
- 2026-03-19: Active Twin Spin now renders its own visual effect: a faint orbit ring plus bright white slash trails around each spinning blade, with intensity that scales slightly from current spin stacks so Sword 17 no longer reads as blade-only while spinning.
- 2026-03-19: Active Twin Spin now separates `visualRange`/`visualDistance` from real hit `range`; the hit radius is recalculated from the visible blade-tip distance of the spinning weapons, and the spin trail/ring visuals now use that tip radius too instead of the handle orbit. The active Twin Spin radius is also scaled up another 25% from the previous baseline.
- 2026-03-19: Active Twin Spin slash trails now read `resolveMeleeSwingBladePose(...).tipX/tipY` per blade instead of using the raw spin angle alone, so the white slash arc is anchored to the visible blade tip rather than drifting toward the hilt.
- 2026-03-19: The rebuilt normal-swing slash visual now anchors directly to the outer edge of `player.swing.range`, drawing a short bright annular slice at attack distance instead of trying to derive the effect from sword-local geometry.
- 2026-03-19: Melee swing rendering now has a dedicated `resolveMeleeSwingBladePose(...)` helper plus default sword `tipLocal` scales, so future swing visuals can anchor directly from the visible blade tip (`tipX/tipY`) instead of the player center.
- 2026-03-19: The experimental normal-swing crescent slash visual was removed again so melee swing rendering is back to blade-only before rebuilding a cleaner version from scratch.
- 2026-03-19: The sword-swing slash trail was pushed much farther forward from the blade and its radius was reduced, specifically to stop the crescent from visually wrapping around the sword itself.
- 2026-03-19: The swing slash center now follows the sword position itself instead of staying centered on the player combat anchor, so outward radius changes actually move the effect on screen.
- 2026-03-19: The annular swing trail radius was pushed farther outward again so the white slash sits more clearly ahead of the blade instead of reading from the sword midpoint.
- 2026-03-19: The swing slash visual was revised again to follow the blade path as a trailing annular crescent around the combat anchor, with mirrored trail direction for dual-cross swings, replacing the detached moon-like chunk.
- 2026-03-19: The normal-swing slash visual was revised from a center-anchored arc stroke into a forward-offset filled crescent, so the white air-slice now sits ahead of the blade instead of reading as a glow stuck in the middle of the swing.
- 2026-03-19: Normal melee swings now render a white crescent air-slice that follows the live swing angle, including dual-cross swings, without changing hit timing or damage logic.
- 2026-03-19: Utility flying swords now move with stored velocity plus acceleration/deceleration instead of constant step travel; `Power/Attack` scales their thrust acceleration, so launch, redirect, and return behavior should show more real inertia across all flying-sword paths.
- 2026-03-19: Player movement now uses velocity + acceleration/deceleration instead of instant position stepping; top speed still comes from `Speed/agility`, while acceleration is now driven by `Power/Attack` so stronger builds ramp into movement faster.
- 2026-03-19: Starter weapon selection now uses the same reroll allowance path as level-up rewards, so the existing starter popup reroll button is visible and usable too.
- 2026-03-19: Sword 12 utility beam no longer waits through a pre-fire charge delay, and its beam-active sword visual now uses a direct gold fill wash on the blade sprite instead of reading mainly as an outer gold rim.
- 2026-03-19: Sword 12 utility beam charge no longer uses small surrounding particles; the flying sword now sells the charge mostly through a stronger gold-tinted blade wash and warm sword-local glow.
- 2026-03-19: Sword 12 utility beam now has a real pre-fire charge state; during `charging` the flying sword renders a stronger white-hot aura, brighter blade wash, and charge particles around/toward the tip before the beam line appears.
- 2026-03-19: Added Sword 12 passive `Prism Beam` as a hold-to-channel piercing beam skill for hand slots, with canvas beam rendering, mana drain, hit logic, tooltip copy, and text-state exposure for future tuning.
- 2026-03-19: Reduced monster spawn rate to `2/sec` in play runtime.
- 2026-03-19: Dead monsters now despawn immediately instead of lingering after death.
- 2026-03-19: Every level up now queues a 3-choice reward popup with random weapons/artifacts, and the current reward selection persists through save/load.
- 2026-03-19: Utility flying swords now trace a 6-point star route around their target with light homing toward the hitbox instead of pure straight back-and-forth thrusts.
- 2026-03-19: Sword 17 now uses a new hand-slot hold passive `Titan Edge`; holding Attack drains mana continuously, scales Sword 17 melee range in real time, and leans on the existing range-to-size coupling so the blade swells visually while auto-repeating slashes. Sword 12 beam still keeps hold priority if both swords are equipped together.
- 2026-03-19: Titan Edge input was split into `tap = normal slash` and `hold = channel growth` so Sword 17 no longer fires a normal swing immediately on mouse-down; short taps still slash, but holding now reliably enters the mana-fed growth state before auto-repeat kicks in.
- 2026-03-19: Added a visible Titan Edge channel pose for Sword 17 while holding Attack but before/without an active swing, so the sword now visibly swells and glows during the mana-fed hold state instead of feeling idle until the first repeated slash fires.
- 2026-03-19: Utility flying swords now thrust through to the far side of a target, turn back for a return stab, and then rotate to the next of 6 angles.

## Working Features
- Character slots: create, select, delete, save/load
- Play scene: movement, facing, HUD, settings
- Dummy targets + DPS readout
- Inventory UI with fixed baseline layout
- Unified box container system
- Drag/drop and `Shift+click` transfers
- Ritual page:

## Recent Updates
- 2026-03-18: Reduced monster total stat scaling from `7x` player core stat total to `2x` in `js/systems/entities.js`.
- 2026-03-18: Added a fixed start circle around the player spawn point; the run only becomes active after leaving that ring.
- 2026-03-18: Monster spawning now starts only after the run begins, spawning at `5/sec` and doubling the spawn rate every `10` in-run minutes.
- 2026-03-18: Active monster state now restores saved monster positions, despawns dead monsters after a short fade, and drops monsters that drift too far from the player.
- 2026-03-19: Monster runtime now streams by viewport: loaded monsters are capped at `1000`, off-screen monsters unload immediately, and new monsters spawn inside the current player view instead of accumulating out of sight.
- 2026-03-19: Play HUD overlays (`inventory`, `stats`, `settings/save`, and starter weapon selection) now hard-pause the run: world time stops, movement/combat/spawn/regen freeze, pointer combat input is gated, and real-time-based timers are offset on resume so charge/stack decay do not skip ahead.
- 2026-03-19: Weapon stat scaling is now routed through shared combat helpers so weapon damage/range/speed all read player-derived stats consistently; HUD, hover tooltips, and starter-weapon cards now display the scaled values instead of raw weapon-only numbers.
  - left detail panel
  - right skill library
  - 3 bottom chosen-skill slots
  - top sigil color follows the focused skill

## Inventory Baseline
- Use the third inventory reference as the fixed base layout for all inventory-style panels
- Weapon hands: 2 slots
- Utility: 6 slots
- Stash: 24 slots
- Chest layouts: 6 columns baseline
- Boxes are generic containers now; any inventory item can move into or out of an open box
- Open box UI renders only the rows needed for current contents, while container capacity remains `120`

## Weapon State
- Active weapon ids only: `7, 9, 11, 12, 15, 16, 17, 19`
- Only these files remain in `asset-use/weapons/`

## Artifact State
- Active artifact ids only: `12, 13, 14, 27, 29, 30, 32, 35, 36, 40, 43, 44, 47, 48, 55, 56, 57, 58, 62, 63, 64, 65, 70, 71, 73, 77, 79, 80, 81, 83, 89, 91, 93, 96, 98, 99, 100`
- Only these 37 files remain in `asset-use/artifacts/`

## Recent Decisions
- Ritual lower UI was separated more clearly from the background
- Weapon catalog was reduced to the active set only
- Old progress log was archived to [progress_archive_2026-03-17.md](/Users/mon/roguelike-endless-skill/progress_archive_2026-03-17.md)
- Melee swing/spin hit detection no longer tracks `dummy` ids directly; combat now resolves against generic target keys collected from entities in attack range.
- Single-swing state now stores `targetKeysInRange` and `hitTargetKeys`, and `render_game_to_text` exposes swing `targetCount` / `hitTargetCount` for deterministic combat checks.
- The bundled Playwright client produced flaky hangs when testing through a redirecting seed page, so combat verification used direct Playwright evaluation after still exercising the official client.
- `Shift+click` inventory transfer now runs on `mousedown` instead of waiting for `click`, and the old 140ms suppress window was removed from quick-transfer paths.
- Added one-shot `pendingInventoryClick` suppression so the trailing `click` after an immediate `Shift+mousedown` does not re-handle the same slot.
- Verified rapid stash <-> container transfers with deterministic Playwright evaluation: two consecutive `Shift+mousedown` actions now move both items immediately and do not get blocked by `dragSuppressUntil`.
- Player stat formulas were moved into [js/systems/stats.js](/Users/mon/roguelike-endless-skill/js/systems/stats.js), and all pages now load that file before combat/play so stat math is no longer split between `script.js` and `combat.js`.
- Inventory domain helpers were moved into [js/systems/inventory.js](/Users/mon/roguelike-endless-skill/js/systems/inventory.js), and all pages now load it before combat/play so stash/container/loadout work can be edited without reopening the whole shared core file.
- Play runtime was trimmed to avoid constant HUD/settings DOM writes: `renderPlayingHud()` now snapshots visible values, skips redundant updates, and no longer re-syncs settings controls every frame.
- Inventory panel rendering now uses summary/full snapshots so drag/select/overlay paths do not rebuild the same DOM repeatedly, and drag/drop highlight cleanup now tracks the active source/target nodes instead of querying the whole document every move.
- Scene rendering now caches the menu backdrop base layer, the visible dungeon floor tile slab, and visible torch cell seeds so the canvas path no longer re-hashes and redraws the same static geometry every frame.
- DPS tracking now keeps a running damage total instead of reducing the full damage event list every update tick.
- Added `js/systems/entities.js` as the future entity hub and seeded 10 ambient monsters around the player using idle monster assets from the pack; these monsters have their own stats/save payload but are not yet wired into combat targeting, hostility, or attack logic.
- Monster core stats now use a shared base value of `90` with per-monster plus/minus offsets inside `js/systems/entities.js`; runtime values like `maxHealth`, `attack`, `defense`, `moveSpeed`, and `awarenessRadius` are derived from that stat block instead of being hard-coded separately.
- Monster runtime values are now balanced against the current player reference on session start/load, so the same `90 +/-` core stat block scales with player health, attack, defense, movement speed, and scene tile size instead of staying fixed forever.
- Monsters now expose a derived hitbox block and render a visible hitbox outline plus health bar in play view; hitbox scale data is also preserved in save payloads and exposed through `render_game_to_text`.
- Monster hitbox overlay is now hidden from players by default; hitbox data still exists for combat checks and debug/state output.
- Monster target-like hover motion is now hidden from players by default so monsters stay visually stable while aiming; hitbox, damage, and health-bar rendering stay active.
- Monster target brackets were found to be baked into the monster sprite frames themselves, so play rendering now strips those corner pixels into a cached clean frame before drawing; this hides the red target corners without editing the source assets.
- Player level progression now has infinite doubling EXP requirements using `BigInt` (`1, 2, 4, 8, ...`), grants 5 stat points every level-up, persists EXP/stat points in save data, and shows level progress as a circular portrait ring with the level number anchored at the bottom-left of the portrait.
- Player stats now have spendable core stats (`vitality`, `power`, `guard`, `agility`, `instinct`) on a base-10 scale for the visible character sheet; runtime combat/HUD values are derived from those core stats so the small displayed values and internal larger values stay proportionally aligned.
- Added `+` buttons in the stats panel to spend stat points immediately in-run, wired through save/load and `render_game_to_text`; old saves without `coreStats` are migrated by estimating a 10-scale core stat profile from their previous derived values.
- Visible player core-stat terminology in the HUD now uses simpler labels for readability: `Health`, `Attack`, `Defense`, `Speed`, `Control`; internal save/runtime keys remain `vitality`, `power`, `guard`, `agility`, `instinct`.
- Fullscreen hotkey handling is now more reliable: it toggles from physical `KeyF`, ignores key-repeat and modifier combos, avoids stealing focus from text inputs, and serializes fullscreen enter/exit requests so `f` no longer feels random.
- Play page startup is lighter now: `Phaser` was removed from the critical script path on `play.html`, hidden inventory DOM is no longer fully rendered during `startPlayingSession()`, and floor/torch/monster-frame cache warming now runs after the first frame instead of blocking the entry moment.
- Startup-smoothing follow-up: `startPlayingSession()` now enters on a lighter first frame by deferring monster restore/spawn and starter-weapon popup DOM into the warmup callback, lazily allocating the floor cache canvas, and skipping the expensive floor/torch scene pass until the warmup marks the render state as ready.
- Artifact follow-up: `Artifact 12` now carries a real equipped passive (`Far Reach`) that grants `+50%` skill range while slotted in utility, and player derived stats are rebuilt on equipment refresh so the bonus applies and disappears immediately when that artifact is equipped or removed.
- Skill-range follow-up: a shared `playerSkillRangeMultiplier()` now turns `player.skillRange` into a reusable gameplay multiplier, and current non-basic actions (`Twin Spin` and utility-sword auto-attacks) now read that multiplier for their real attack distance instead of only showing the larger value in HUD text.
- Artifact-speed follow-up: `Artifact 13` now grants a real equipped `+50% action speed` passive, exposed as `player.actionSpeed`, and combat timing now reads that stat for swings, spin rate/hit cadence, and utility-weapon attack timing while player movement continues to use agility-only move speed.
- Artifact-lifesteal follow-up: `Artifact 14` now grants a real equipped `2% lifesteal` passive based on final resolved damage dealt, applied centrally in `applyDamageToCombatTarget()` so all current player attack paths heal from post-mitigation damage without touching movement or regen rules.
- Heal-feedback follow-up: action-based player healing now runs through shared `applyPlayerHealing()` / `queuePlayerHealFloat()` helpers, and play rendering shows green floating `+heal` numbers above the player even when the heal is fully overheal-clamped at max HP.
- Player-float follow-up: the player-side popup numbers were generalized into a shared `combatFloats` stack, so healing stays green with `+` and incoming player damage can now use the same stacked renderer in red with `-` through the new `applyPlayerDamage()` / `queuePlayerDamageFloat()` helpers.
- The active UI flow now reads more internationally: visible Thai copy in `create`, `ritual`, `play`, dynamic HUD/inventory/save messages, risk labels, and ritual category tags was converted to clearer English, while already-good English wording was kept.
- Added [js/systems/damage.js](/Users/mon/roguelike-endless-skill/js/systems/damage.js) as the shared damage formula hub, wired player swing/spin through it, and promoted monsters into `getCombatTargets()` so player attacks now resolve real damage against monster health using defense mitigation.
- `create.html` was rebuilt into a simpler two-column `preview + form/readout` layout, the broken commented `origin` block was removed, and the page now keeps only the fields actually used by the current flow (`name`, `gender`, confirm/back, controls) while preserving the existing forest/ritual theme.
- `create.html` no longer loads page-irrelevant heavy scripts like the gameplay systems stack; it keeps the shared core plus `play.js/pages.js/bootstrap.js` needed for the shell and route flow.
- The create page was then stripped down further: explanatory taglines, notes, preview pills, ritual forecast/readout, and control hints were removed so the screen now relies on the flow itself instead of tutorial copy.
- UI direction now follows the current `create.html` reference more closely across the active flow: `select` uses shorter slot language and cleaner detail cards, `ritual` drops hidden/visible helper copy and trims its script payload, and `play` now shows terse panel headings without form-like helper text.
- Empty slot/state wording was normalized to match the visible UI (`Empty Slot`, `Create`, `Enter`) so `render_game_to_text` and other debug payloads no longer report stale labels from the older flow copy.

## Best Next Work
- Connect ritual-selected skills into real in-run gameplay
- Add real enemies beyond dummy
- Add EXP / level up / upgrade pause loop
- Keep UI work secondary unless it blocks readability
- Next combat step should be adding hostile behavior / target intent on top of the shared `getCombatTargets()` path that now already includes monsters and dummies.

- Inventory container refactor: ยุบ weapon chest + artifact chest ให้เป็นกล่องแบบเดียวใน data, UI, drag/drop, และ quick transfer
- Inventory layout bugfix: เมื่อเปิดกล่อง ฝั่งขวายังเป็น `loadout + stash` ตายตัว และก้อนดำด้านล่างถูกตัดออกโดยให้กล่องแสดงเฉพาะจำนวนแถวที่จำเป็น

- Ritual UI: ลดความมืดของฉากหลังและทำ backplate ของ shell/slot/action ชัดขึ้นเพื่อแยกพื้นหลังกับกล่อง UI ออกจากกัน

- Inventory bugfix: บังคับ normalize ความยาวของ stash/weapon/utility/chest ทุกครั้งก่อน render และตอนเริ่ม session เพื่อกัน save เก่าหรือ state เพี้ยนจนช่องหายทั้งก้อน
- Inventory bugfix: เพิ่ม min-height ของ stash grid ให้แสดงฐาน 3 แถวชัดเจนแม้ของข้างในว่างทั้งหมด

- Inventory layout bugfix: ล็อก row ของ inventory layout ตอนเปิดกล่องเป็น max-content และบังคับ card ไม่ให้ stretch ตามแถว เพื่อกัน stash card ล่างขวาถูกยืดเป็นกล่องดำสูงเกินจริง

- Inventory layout refactor: ย้าย loadout + stash เข้า inventory-player-column เดียวกัน แล้วให้ chest/artifact เป็น sibling ต่างหาก เพื่อให้ stash อยู่ใต้ loadout ตลอดและไม่หายเมื่อเปิดกล่อง

## 2026-03-18 UI Pass
- Updated [select.html](/Users/mon/roguelike-endless-skill/select.html), [ritual.html](/Users/mon/roguelike-endless-skill/ritual.html), [play.html](/Users/mon/roguelike-endless-skill/play.html), [script.js](/Users/mon/roguelike-endless-skill/script.js), [js/systems/play.js](/Users/mon/roguelike-endless-skill/js/systems/play.js), [styles.css](/Users/mon/roguelike-endless-skill/styles.css), and [styles/play.css](/Users/mon/roguelike-endless-skill/styles/play.css) to keep the active flow closer to the user's current `create.html` tone.
- `select` now uses `Empty Slot / Create / Enter` wording, simpler archive chips, and no control helper/footer copy.
- `ritual` now uses shorter search/action labels, a cleaner detail card, no hidden controls text, and only loads the lightweight page scripts it actually uses.
- `play` now removes the explanatory stat/settings copy and stat descriptions, uses shorter internal inventory/loadout strings, and gives panel titles a more in-theme serif treatment.
- Syntax checks passed for [script.js](/Users/mon/roguelike-endless-skill/script.js) and [js/systems/play.js](/Users/mon/roguelike-endless-skill/js/systems/play.js).
- Local visual checks succeeded for `select`, `create`, and `ritual` via Playwright full-page screenshots in `/Users/mon/roguelike-endless-skill/output/ui-flow-check/`.
- The official bundled Playwright client still captured the select route, but in this environment it can hang after launch and only save the canvas layer, so HTML overlay verification used direct Playwright full-page screenshots instead.
- Full-page automation consistently reached `ritual`, but the handoff into `play` still hangs under headless Playwright before a reliable screenshot is produced; the code changes for `play` were therefore verified by syntax review rather than a completed visual capture.

## 2026-03-18 Play Start Pass
- Training dummies were removed from the live play session setup by changing `startPlayingSession()` to start with `dummies: []`, while the older dummy/combat code paths were intentionally left in place for possible reuse later.
- Added a start-of-run weapon choice overlay in [play.html](/Users/mon/roguelike-endless-skill/play.html) and [styles/play.css](/Users/mon/roguelike-endless-skill/styles/play.css): three random weapon cards appear in a row and the run stays input-blocked until exactly one weapon is claimed.
- Added starter-weapon flow logic in [js/systems/play.js](/Users/mon/roguelike-endless-skill/js/systems/play.js) plus the click binding in [js/systems/pages.js](/Users/mon/roguelike-endless-skill/js/systems/pages.js). Claiming a card equips it directly into weapon slot 1 and marks the run as already having chosen its starter weapon.
- Persisted `starterWeaponClaimed` in [script.js](/Users/mon/roguelike-endless-skill/script.js) so the popup does not reopen on later loads, and exposed the popup state / options in [js/bootstrap.js](/Users/mon/roguelike-endless-skill/js/bootstrap.js) for future debugging.
- No behavior tests were run for this pass because the user explicitly asked to return to the normal no-test workflow.
- Fixed a play inventory hover regression: a page-level hover override had accidentally applied the menu button's `translate(-50%, -2px)` transform to inventory/loadout slots, so hover states could jump sideways. Inventory hover and selected/active visuals are now separated again, and empty slots no longer render a persistent active highlight.
- Follow-up visual tweak: removed the green tint from item hover in play inventory/loadout so hover now uses the warmer gold-brown accent instead of reading like a selected or buffed state.
- Follow-up cleanup: removed the remaining green fill/border from `quality-green` inventory and loadout slots during normal hover/display so weapon rarity no longer leaks a green hover-like panel tint.
- Final follow-up on the same pass: active/select highlights for loadout and stash slots were also moved off green and onto the warm gold-brown palette, so equipped-slot focus no longer reads as a green state either.
- The old item-quality split was then removed from the active code paths entirely: weapon/artifact/item data no longer carries `quality`, inventory/starter-weapon rendering no longer emits `quality-*` classes or data attributes, and the remaining setup-page quality-dot styles were deleted as dead code.
- Added an in-run inventory hover detail card: hovering a filled stash/loadout/chest slot now opens a themed floating panel with item name, category, location, and quick stats. The UI lives in [play.html](/Users/mon/roguelike-endless-skill/play.html) / [styles/play.css](/Users/mon/roguelike-endless-skill/styles/play.css), and the behavior is delegated from [js/systems/pages.js](/Users/mon/roguelike-endless-skill/js/systems/pages.js) into the inventory helpers in [js/systems/play.js](/Users/mon/roguelike-endless-skill/js/systems/play.js).
- Inventory loadout preview was then reshaped into a narrower vertical frame and the preview sprite scale was reduced so the full player body can fit inside the center showcase instead of reading like a wide cropped bust.
- Expanded the loadout utility area from 6 to 8 slots and widened the right-side grid to 4x2 so the narrower portrait layout uses the freed space cleanly.
- Shifted the player's attack reference point from the head area to a shared combat anchor on the torso so aim, melee range checks, and weapon visuals all originate from the body instead of floating high.
- Reduced play-scene render churn by caching the static base background, the vignette overlay, and reusable torch light sprites so the game no longer rebuilds those gradients every frame.
- Added a shared mana-spend path with a short regen lock so mana recovery no longer ticks immediately while or right after mana is being consumed.
- Simplified mana recovery again so regeneration keeps ticking even while mana is being spent, matching the easier-to-read player-facing rule.
- Bound the spin ability to Sword 17 only when both weapon slots hold that weapon, and surfaced its passive description plus stack behavior in the weapon hover details and starter weapon picker.
- Relaxed Sword 17's passive requirement so `Twin Spin` now activates while dual wielding with Sword 17 in either hand, instead of requiring both hands to be Sword 17.
- Weapons placed in the 8 utility slots now orbit the player visually in a vertical loop; artifacts stay ignored there for visuals, and the orbit is presentation-only for now.
- Rotated the visual-only utility weapon orbit another 90 degrees so the orbiting weapon sprites stand in the direction requested on screen.
- Converted the utility-weapon orbit from an ellipse to a circle and flipped the sprite orientation another 180 degrees to match the requested facing.
- Shift+click from stash now auto-equips into the first valid empty slot when no box is open, prioritizing the 2 weapon hands before the 8 utility slots.
- Added a support-only utility-weapon passive path: Sword 17 in the 8 utility slots no longer enables spin itself, but any mana spent now feeds Twin Spin stack gain through the shared mana-spend flow.
- Twin Spin stack gain now flows only through the shared mana-spend passive framework, so hand + utility copies add together cleanly instead of double-firing from a separate spin-only path.
- Melee weapons placed in the 8 utility slots now use a shared `utilityCombat` profile and automatically deal damage while orbiting, giving the utility system a reusable auto-attack path for future weapon types.
- The utility melee auto-attack path was then upgraded from simple orbit collision into a reusable thrust system: each slot keeps its own attack state, locks onto hostile targets automatically when launching, flies out and back in a straight line using the blade tip, travels `10x` the player radius per thrust, and deals damage to any combat hitbox the swept tip segment crosses instead of requiring the locked target itself to be the one hit.
- Follow-up fix: utility thrusts now only launch if a hostile target is inside their real player-centered attack radius, and if the locked target leaves that radius mid-flight the weapon immediately breaks off and returns instead of chaining attacks forever.
- Visual follow-up: utility thrust attacks now keep the same fixed upright sprite rotation as the idle orbit instead of turning with the target direction, so the flying blade no longer looks slanted mid-attack.
- Visual anchor follow-up: the flying utility-weapon sprite is now positioned from a fixed tip anchor instead of subtracting the current flight direction vector, so off-angle thrusts keep the blade tip on the attack line instead of drifting into a slanted-looking offset.
- Final thrust-anchor follow-up: the utility weapon tip anchor now rotates with the real flight direction before positioning the sprite, so the blade tip auto-adjusts across different angles instead of only looking correct on one heading.
- Range follow-up: utility thrust travel distance is currently tuned to `30x` the player radius for melee weapons in the 8 utility slots unless a weapon overrides that multiplier later.
- Target-chain follow-up: utility swords now stay in attack mode, repeatedly passing through the same locked hostile until it dies, then immediately retarget the nearest hostile to the player without returning home; they only enter return flight when no hostile remains inside the player-centered attack radius.
- Pass-distance follow-up: utility sword repeat-stabs now enforce a much longer minimum pass distance around the locked target, derived from the weapon utility combat profile instead of only the target hitbox size, so the back-and-forth motion reads cleanly on screen.
- Reverted the experimental 6-angle / star-route utility sword flight behavior and restored the earlier straight pass-through thrust path because the angled variants were not reading well in play.
- Artifact 12 follow-up: the +50% range bonus now also applies to normal melee attack range through `weaponRangeValue()`, not just skill-like range paths.
- Sword 17 follow-up: when used as a utility flying sword, its local Twin Spin range/visual distance are now reduced to roughly 20% of the old size.

- 2026-03-19: Sword 12 beam range now resolves to the current arena edge along the aim direction instead of using its old fixed reach multiplier when arena bounds are available.
- 2026-03-19: Sword 12 beam visuals were pushed toward a white-hot look, with a straighter beam body and jittering beam particles replacing the old ripple-like wobble.
- 2026-03-19: Sword 12 charge preview now gathers particles from around the player into a bright orb at the beam origin before firing, instead of showing a pre-fire beam line.
- 2026-03-19: Sword 12 hold input now enters an aimed sword stance instead of swinging immediately; quick release falls back to a normal slash, and charge particles/orb now gather at the sword tip.
- 2026-03-19: Fixed a Sword 12 charge-preview runtime freeze caused by using `combatAnchor` before defining it; movement/facing should no longer lock up when holding attack.
- 2026-03-19: Beam charge/origin anchoring now uses the sword sprite tip instead of a centerline offset; both the charge orb and fired beam start are aligned to the same tip anchor.
- 2026-03-19: Beam charge pose now references the sword slash distance instead of a hand-held stance, so the sword can float along the attack line while charge/origin still follow the same beam tip anchor.
- 2026-03-19: Sword 12 beam tip anchoring was corrected using the actual sprite tip position from sword-12.png instead of the old generic diagonal guess.
- 2026-03-19: Sword 12 beam firing now draws the sword from the same beam pose used during charge instead of snapping the sprite onto the beam origin.
- 2026-03-19: Doubled Sword 12 tip local offset scales again so the charge orb and beam origin sit farther out toward the blade tip.
- 2026-03-19: Sword 12 now draws a beam-colored weapon-edge highlight while charging and firing, using the same warm white / prism-gold palette as the beam so the weapon reads as energized during skill use.
- 2026-03-19: Refined Sword 12 beam weapon highlighting to use a single-position glow pass instead of multiple offset sprite copies, preventing the energized edge from reading like duplicated overlapping swords.
- 2026-03-19: Stopped treating the current viewport-sized `arena` as a real map boundary for Sword 12 beam range; Prism Beam now falls back to its skill range until true world bounds exist, instead of visibly stopping at the screen edge.
- 2026-03-19: Sword 12 Prism Beam now uses a fixed travel length of `15 tiles` from its beam origin instead of the shorter dynamic skill-range fallback.
- 2026-03-19: Sword 12 Prism Beam now ramps with channel time: damage is recalculated from `weapon damage x total mana spent x channel time`, while mana drain compounds at `1.18x` per second during the channel instead of staying flat.
- 2026-03-19: Tightened Sword 12 beam runtime rules: the channel now stops as soon as a mana tick cannot be paid in full, beam damage width is aligned much closer to the visible beam body, and fast mouse sweeps now use a previous-line sweep check so targets crossed between frames still take damage.
- 2026-03-19: Raised the player's baseline mana regeneration from `1` to `5` per second in the shared stat progression, while keeping it scaled by the `Control` core stat as before.
- 2026-03-19: Sword 12 now mirrors Sword 17's utility behavior pattern: when placed in a flying utility slot and actively attacking a locked target, it auto-channels Prism Beam from the flying blade itself using the wielder's mana, with local beam runtime state and hover readout.

- 2026-03-19: Level-up reward popup now has a `Reroll` action with 2 rerolls per selection, persists reroll count through save/load, and reuses the existing reward overlay without affecting the starter-weapon flow.

- 2026-03-19: Sword 12 utility beam swords now keep chaining to the nearest player target after each single pass and only return home when no hostile target remains in range.

- 2026-03-19: Optimized Prism Beam rendering by caching glow sprites, capping beam particle draw counts, and smoothing held-beam angle updates to reduce visible stutter during use.

- 2026-03-19: Added a persistent graphics quality setting (`Low / Normal / High`); Prism Beam now uses the heavier pre-optimization particle/glow path only on `High`, while lower settings keep the cached lighter renderer.
- 2026-03-19: Sword 12 utility Prism Beam now gets its own intentional white-hot aura layer around the flying blade while active, with cached glow motes and a centered additive sprite wash so the blade reads like it is engulfed in beam energy instead of only relying on incidental overdraw from the beam body.
- 2026-03-19: Refactored Prism Beam into `js/systems/beam.js` so Sword 12 / beam work no longer lives inside the main combat file; `combat.js` dropped to ~2069 lines while beam-specific profiles, runtime, hit sweep, sword poses, glow caches, and beam visuals now live in a dedicated ~1402-line file that loads before combat on pages that include combat.
- 2026-03-19: Removed the unused Phaser layer entirely: `js/systems/phaser-runtime.js` and `js/vendor/phaser.min.js` were deleted, `bootstrap.js` now always starts the native requestAnimationFrame loop directly, `phaser` was removed from `package.json` / lockfile, and legacy redirect pages (`setup`, `start-run`, `briefing`) were trimmed down to only `script.js + pages.js + bootstrap.js` instead of loading the whole gameplay stack.
- 2026-03-19: Twin Spin slash trails now spawn from the blade tip and trail opposite the tip's real motion vector instead of following a guessed orbit tangent; Playwright screenshots in `output/spin-tip-check/` confirmed the white streak now leaves from the visible blade tip much more cleanly.
- 2026-03-19: Refined Twin Spin again so the white slash is now a shorter sampled motion trail taken from several prior tip positions, replacing the thicker detached crescent with a cleaner streak that hugs the blade's real spin path.
- 2026-03-19: Removed the separate Twin Spin orbit ring overlay and kept only the sword sprites plus their sampled white motion trails, which reads cleaner in screenshots while preserving the current slash style.
- 2026-03-19: Removed the old mutual-exclusion guard between Sword 12 Prism Beam and Sword 17 Twin Spin; dual-wielding `12 + 17` now allows both effects to run from the same held attack input, and Playwright verification confirmed `player.beam` and `player.spin` are active together.
- 2026-03-19: When `12 + 17` run together, player beam visuals now anchor to the spinning Sword 12 itself instead of spawning a third duplicate sword sprite; Twin Spin draws Sword 12 with the beam highlight while Prism Beam uses that spinning blade tip as its live origin.
- 2026-03-19: Added the first simple monster AI pass in `play.js`: active monsters now use their own `awarenessRadius`, `moveSpeed`, `attack`, and `stats.agility` to chase the player, stop at melee reach, and attack on a per-monster cooldown via `applyPlayerDamage()`. This is intentionally minimal and stat-driven, with no pathfinding or special attack animations yet.
- 2026-03-19: Monster AI no longer checks awareness before chasing; active monsters now always know the player's position. Their chase speed is also scaled down to `50%`, and the first monster-attack damage pass was reduced by `90%` for safer baseline tuning.
- 2026-03-19: Play view was widened by reducing the base scene `tileSize` in `buildSceneLayout()`, so the player can see more of the arena at once.
- 2026-03-19: Monster runtime `maxHealth` is now cut to `50%` after the player-scaled vitality calculation, while keeping monster attack/defense/move-speed scaling unchanged.
- 2026-03-19: Monster balance reference now reads the player's baseline runtime stats derived from `level + coreStats` instead of current live buffed values, so temporary stack buffs (like accumulated physical enhancement / spin stacks) no longer increase monster scaling.
- 2026-03-19: Utility flying swords now keep the new high acceleration/deceleration profile for livelier motion, but the experimental hard-turn snap was removed again so redirection still has organic inertia instead of pivoting too sharply.
- 2026-03-19: Sword 17's `Twin Spin` was soft-disabled through config (`SPIN_PASSIVE_ENABLED = false`) without deleting any underlying code. Combat passive-source collection, utility twin-spin activation, and UI tooltip/reward passive copy now all respect the disabled state, so Sword 17 no longer triggers spin in-hand or while flying, but the full feature can be restored later by re-enabling the flag.
- 2026-03-19: Added a dedicated `player.beamScreenShake` channel for Sword 12 Prism Beam so the world now shakes continuously during both beam charge and active firing, stacks cleanly with the one-shot Sword 17 finisher shake, and stops immediately when the held beam is cancelled or released.
- 2026-03-19: Swapped Sword 12 Prism Beam shake feel between charge and active fire for tuning: charge now uses the heavier/stronger shake profile, while the active beam uses the lighter profile so the user can compare which mood reads better.
- 2026-03-19: Applied the same Sword 12 shake profile directly to Prism Beam visuals too: charge orb/tip energy now jitters with the charge shake state, and the active beam body/endpoints render with a lighter per-end screen-space wobble while hit logic still uses the stable underlying beam line.
- 2026-03-19: Extended the same charge-vs-active shake language to Sword 12 utility Prism Beam as well: flying-beam body/endpoints now wobble visually like the held beam, and the utility beam aura/weapon glow jitters with the same charge-active profile without changing the stable damage line.
- 2026-03-19: Utility Prism Beam visuals now lock to the flying sword more tightly: the sword pose reads `utilityBeamState.angle`, charging stores that angle before firing, and the utility beam line uses a shared whole-line shake instead of different start/end wobble so the blade now turns with the beam rather than the beam seeming to steer away from the sword.
- 2026-03-19: Removed Prism Beam visual shake again from every path. Held charge orb, held beam body, flying-beam aura, and flying-beam body now render from stable beam/tip positions, while `player.beamScreenShake` remains as the only shake channel for Sword 12.
- 2026-03-19: Important reference note for future beam work: Sword 12's sprite art is authored with a built-in right-leaning tilt, so both held-beam and utility-beam sword poses must keep the extra `+45deg` rotation compensation. If that offset is removed, the blade will look like it is aiming ~45deg off even when the beam logic is correct.
- 2026-03-19: Generalized sprite-tilt handling into per-weapon data instead of a one-off Sword 12 fix. `MELEE_SWORD_PROFILE` now carries `rotationOffsetRadians`, melee/beam pose resolvers both read `rotationOffsetRadians`, and Sword 12 stores its offset in weapon data so future swords with pre-tilted art can be corrected by config instead of math edits.
- 2026-03-20: Fixed a hard-freeze bug for Sword 12 utility beam. `resolveUtilityBeamSwordPose()` was reading `beamSkill` before declaration during flying-beam fire, which could throw at render time and freeze the player/game loop; the resolver now declares `beamSkill` before using it in the rotation path.
- 2026-03-20: Widened the play view more aggressively by scaling `buildSceneLayout()` tile size down to `base * 0.5` (with a `34px` floor). The earlier `base / 1.5` pass was visually too subtle, so the play view now zooms out much harder from the same camera logic.
- 2026-03-20: The previous zoom-out pass still looked unchanged because player sprite scale was still tied to arena dimensions instead of scene tile size. Added `playerSpriteScaleForScene(scene)` in `play.js` and now both initial load + canvas resize derive player sprite scale from `scene.tileSize`, so wider camera changes finally read on screen.
- 2026-03-20: Reduced player sprite scale another 50% by halving `playerSpriteScaleForScene(scene)` from `tileSize * 1.6 / 32` to `tileSize * 0.8 / 32` and lowering the floor from `1.4` to `0.7`.
- 2026-03-20: Fixed blinking newly spawned monsters after the zoom-out pass. The extra `despawnDistance = tileSize * MONSTER_DESPAWN_DISTANCE_SCALE` check in `prunePlayingSessionMonsters()` started deleting edge-of-screen monsters immediately once tile size was reduced, so pruning now relies on viewport streaming only.
- 2026-03-20: Level-up rewards no longer auto-open. They now queue silently, render as a separate red circular badge beside the portrait frame, and open only when the user clicks that badge. Once the user is already inside the level-up reward flow, queued rewards still chain one-by-one until the queue is empty.

- 2026-03-20: High graphics Prism Beam visual upgraded with extra halo, filament, endpoint corona, and charge corona layers in beam.js (High only; Normal/Low unchanged).
- 2026-03-20: Tuned Sword 12 held Prism Beam pose and sprite gold without touching beam logic. The beam-specific draw origin now sits slightly farther left (`drawOriginXScale: 0.278`) so the sword reads a little closer/less "popped out", and held charge/active beam drawing in `beam.js` now pushes stronger tint/highlight values so the blade keeps a warmer gold look again.
- 2026-03-20: High-graphics Prism Beam now has animated internal energy currents layered over the existing halo/filaments. The new current lanes stay inside a gold-family palette (amber/champagne/ivory) and flow along the beam body in both held and utility beam paths without affecting hit logic or Normal/Low visuals.
- 2026-03-20: Added a second high-graphics-only Prism Beam overlay layer with a richer gold tone and a phase-shifted lateral drift relative to the white beam core. This is visual-only, applies to both held and utility beam rendering, and intentionally leaves damage/hit lines unchanged.
- 2026-03-20: Densified Prism Beam high-graphics energy currents again. The internal current layer now uses 6 gold-family lanes (up from 4) and 5 moving segments per lane (up from 3), with more distinct amber/champagne/white-gold separation while staying inside the overall gold theme.
- 2026-03-20: Boosted utility flying-sword max speed 5x for every melee sword except Sword 12 by raising default `utilityCombat.flightSpeedScale` from 22 to 110 while keeping Sword 12 at 22 so its beam behavior stays readable.
- 2026-03-20: Added simple utility flying-sword impact streaks. Utility thrust hits now spawn a short double-pointed streak directly on the target aim point, and reaching the thrust destination also spawns a smaller streak at the blade impact position. The visual lives on `player.utilityImpactStreaks`, is rendered in `play.js`, and is visual-only (no hit logic changes).
- 2026-03-20: Reused the new double-pointed impact streak beyond utility thrusts. Non-beam player attacks now spawn the same streak style on hit for normal melee swings, active Twin Spin, and utility Twin Spin, all using the target position directly rather than a destination impact probe.
- 2026-03-20: Added acceleration-only afterimages for utility flying swords in `combat.js`. The effect uses faded copies of the actual sword sprite (not particles), driven by each sword's live velocity delta in `applyUtilityWeaponAttacks()`, and it renders behind both standard thrusting swords and utility-beam Sword 12 using the correct beam sword pose when applicable.
- 2026-03-20: Utility flying-sword thrust damage now scales with lock distance and acceleration stats instead of using only the flat profile damage. On hit, utility thrust now resolves `base utility damage + (normalized thrustAccelerationRate * weaponDamageBase * locked-distance-ratio)` using the distance captured when the sword locks a target, so farther player-to-target locks hit harder while staying bounded by utility travel distance.
- 2026-03-20: Added turn-assist steering to `moveUtilityWeaponTipWithPhysics()`. Utility flying swords now damp lateral velocity, brake harder when they start moving away from the destination, and add extra near-target steering, which should reduce the "orbit around the enemy and fail to reconnect" behavior without bringing back the old hard snap-turn feel.
- 2026-03-20: Utility flying swords now spend mana continuously instead of on launch. Every melee utility sword drains `1 mana per second` while it is actively attacking a target (`phase === "attack"`), and if the player cannot maintain the full per-frame cost the sword immediately cancels aggression and returns home.
- 2026-03-20: Split Titan Centipede boss code into `js/systems/boss.js` so boss profile/summon/control/beam/render no longer sit inside `play.js` or `entities.js`. `play.js` now keeps only the loop/camera hook points, `entities.js` is back to monster-only data, and `CODEX_MAP.md` was updated so next reads can jump straight into boss work with fewer file opens.
- 2026-03-20: Titan Centipede render layering in `js/systems/boss.js` now lifts body segments `1-2` above the fangs only. Their shadows still stay underneath, so the near-head body reads on top without muddying the fang silhouette.
- 2026-03-20: Added Titan Centipede phase hotkeys in `js/systems/boss.js` + `js/systems/play.js`. Pressing keyboard `1`, `2`, or `3` now switches the active boss head phase immediately for fast art/debug checks without needing to resummon.
- 2026-03-20: Titan Centipede phase changes now use the horn/tail-tip cover transition instead of swapping the head instantly. Pressing `1/2/3` queues `retreat to body -> sweep in front of face above the head layer -> swap head phase while covered -> reverse back to the default horn position`, driven entirely from `js/systems/boss.js`.
- 2026-03-20: Phase 3 Titan Centipede beam visuals in `js/systems/boss.js` now keep a visible origin orb and taper into the main beam through a triangular funnel, so the firing silhouette reads more like `orb -> triangle -> beam` instead of a blunt line starting directly at the mouth.
- 2026-03-20: Tightened the phase 3 beam origin shape in `js/systems/boss.js` after visual check feedback. The beam body now uses a flat `butt` start cap for phase 3, skips the old oversized circular origin glow there, and keeps only a smaller orb plus the triangle funnel so the root no longer blooms into a round cap.
- 2026-03-20: Reworked Titan Centipede runtime into a phase-aware ram boss in `js/systems/boss.js`. Movement now uses head-led steering with turn inertia and forward thrust instead of the old omni-directional mover, ram hits deal close-range acceleration-weighted damage similar to utility flying swords, phase 1 now runs at a +50% baseline speed, phase 2 doubles that speed and upgrades close mouth hits into 2x fang bites, and phase 3 massively widens the beam profile.
- 2026-03-20: Phase 3 Titan Centipede beam fire now spreads the fang pair up to `70%` wider in `js/systems/boss.js` by scaling the live firing `fangOffsetY` to `1.7x` at full beam-open, without changing the non-firing baseline pose.
- 2026-03-20: Smoothed the phase 3 Titan Centipede beam body in `js/systems/boss.js`. The beam now keeps soft round outer shells and extra filament/current layers while preserving a flatter core start behind the triangle funnel, so the silhouette stays `orb -> triangle -> beam` but the beam body reads less blocky and angular.
- 2026-03-20: Refined the phase 3 beam smoothing pass so it no longer overwrites the triangle-root silhouette. In `js/systems/boss.js`, the soft shell layers now start slightly later than the core and fade in after the triangle, keeping the `orb -> triangle -> beam` root intact while only softening the body farther down the beam.
- 2026-03-20: Tightened the phase 3 beam root proportions in `js/systems/boss.js`. The triangle funnel is now much shorter and narrower, with its own capped width instead of inheriting the full phase-3 beam thickness, so the beam root reads like a controlled transition piece instead of giant translucent wedges.
- 2026-03-20: Softened the phase 3 Titan Centipede beam root again in `js/systems/boss.js`. The origin triangle now uses slightly curved sides instead of rigid straight wedges, a short transition blend smooths the handoff into the main beam, and the head gets a post-draw beam light overlay so it no longer looks visually detached from the blast.
- 2026-03-20: Increased Titan Centipede fang opening in `js/systems/boss.js` so full beam fire now opens the fangs to `90 degrees` at max `fangOpenProgress`, using a dedicated `TITAN_CENTIPEDE_FANG_OPEN_MAX_ANGLE` constant for faster tuning later.
- 2026-03-20: Clarified the fang-open angle scope in `js/systems/boss.js`. The `90 degree` max is now phase-3-only via `TITAN_CENTIPEDE_PHASE_THREE_FANG_OPEN_MAX_ANGLE`, while other phases keep the older smaller fang-open angle.
- 2026-03-20: Restyled the Titan Centipede phase-3 beam in `js/systems/boss.js` toward the new reference look. The beam root is now longer and narrower, the main blast shifts to a `white-hot core + red/pink edge rails` palette instead of the older blue/gold-heavy mix, the giant round root bloom is toned down, and the head still receives a smaller post-draw light flare so it stays visually connected to the shot.
- 2026-03-20: Verified the new phase-3 beam look locally with Playwright against `http://localhost:4173/play.html?slot=1` by seeding slot 1 through `localStorage`, forcing boss summon/control state, then advancing the simulation with `window.advanceTime(1400)`. Reference snapshots from this pass are saved at `output/web-game/phase3-beam-before.png`, `output/web-game/phase3-beam-after.png`, and `output/web-game/phase3-beam-final.png`.
- 2026-03-21: Split Titan Centipede beam charge timing by phase in `js/systems/boss.js`. Phase 3 now uses `7 seconds` via `TITAN_CENTIPEDE_PHASE_THREE_BEAM_CHARGE_SECONDS`, while earlier phases keep the old short `0.58s` charge.
- 2026-03-21: Slightly sped up Titan Centipede phase-3 fang opening in `js/systems/boss.js`. Phase 3 now uses `TITAN_CENTIPEDE_PHASE_THREE_FANG_OPEN_SECONDS = 0.08`, while other phases keep the older `0.12s` opening pace.
- 2026-03-21: Added persistent phase aura framing to Titan Centipede in `js/systems/boss.js`. Phase 2 now draws a red glowing rectangular aura frame around the front/head section, while phase 3 draws a darker black-charcoal version with a faint ash edge so higher phases read immediately on screen.
- 2026-03-21: Reworked Titan Centipede phase aura rendering in `js/systems/boss.js` after checking Sword 12's aura approach. The boss aura now leans on the same style of layered glow sprites plus lighter/screen edge passes instead of a heavier filled frame, so the phase frame reads more like a weapon-energy aura than a solid overlay block.
- 2026-03-21: Replaced Titan Centipede's phase aura frame with sprite-shaped aura passes in `js/systems/boss.js`. The aura now follows the real silhouette of the head and both fangs instead of tracing a rectangle, using enlarged tinted sprite passes behind those parts so phase 2/3 read as part-shaped energy around the boss front.
- 2026-03-21: Documented the reusable `sprite-shaped aura` pattern in `CODEX_MAP.md` so future boss/weapon aura work can reuse the same flow: resolve real part positions first, then draw enlarged tinted sprite silhouette passes behind the actual parts instead of tracing a generic frame.
- 2026-03-21: Extended Titan Centipede's sprite-shaped phase aura onto the body in `js/systems/boss.js`. The body aura now samples the front `10` segments plus every `4th` visible segment after that, capped at `44` aura parts, so the glow runs down the body without paying the full cost of tinting all `150` body segments every frame.
- 2026-03-21: Optimized Titan Centipede body aura rendering in `js/systems/boss.js` after it felt choppy. The body aura now uses a lighter `style: "body"` preset with much smaller blur usage, only keeps stronger inner glow on the first few sampled parts, and reduces coverage to the front `6` segments plus every `8th` visible segment after that, capped at `20` aura parts.
- 2026-03-21: Corrected Titan Centipede boss-front visuals in `js/systems/boss.js`. The sprite-shaped aura near the head now also covers the horn-base/main head assembly piece by tinting the reused tail-frame attachment behind it, and the active boss beam was reordered so the head light overlay is computed first but the actual beam render is drawn last/on top.
- 2026-03-21: Changed Titan Centipede phase-3 beam motion in `js/systems/boss.js` from a slower swell/pulse to a faster tremor. The mouth/origin geometry now stays much steadier by keeping `endpointRadius` almost fixed and barely moving the origin orb, while the beam width/white-hot core use a higher-frequency mixed-wave tremor so the shot reads as vibrating instead of breathing.
- 2026-03-21: Moved Titan Centipede's `full aura` treatment behind `Graphics: High` in `js/systems/boss.js`. Normal graphics now keep a lighter front-only aura preset around the head assembly, while the heavier body aura pass and stronger blur/inner passes are reserved for high graphics to protect frame rate.
- 2026-03-21: Added the first real `Titan Centipede beam clash` pass across `js/systems/boss.js`, `js/systems/beam.js`, and `js/systems/play.js`. Phase-3 boss beam can now detect a clash with the player's Prism Beam, trim both beams to the clash point for render/hit logic, and render the wide boss beam as two separated streams around the player's smaller center beam so the player reads as carving into the giant beam instead of just line-vs-line colliding. To make this testable in the current boss-control setup, player beam updates/hits are also allowed while controlling the boss if attack input is held.
- 2026-03-21: Added a real `boss dismount` flow in `js/systems/boss.js`. Pressing `B` while already controlling Titan Centipede now releases control immediately even if the frozen player body is no longer within interact range, clears boss/player attack hold state, and repositions the player beside the head so the camera does not snap back to the stale pre-control location.
- 2026-03-21: Relaxed the new `boss dismount` flow in `js/systems/boss.js` for testing. Pressing `B` to release control no longer wipes ongoing attack/beam states; it now cancels movement only, keeps the player beside the head for camera continuity, and temporarily locks boss movement while a held boss beam action is still in progress so beam/clash tests can continue after dismount.
- 2026-03-21: Corrected the first Titan Centipede clash pass after in-game feedback. `resolveBeamSegmentIntersection()` in `js/systems/boss.js` now handles collinear/opposed beam lanes instead of only true line-cross intersections, so straight-on beam tug-of-war can enter clash state and split the phase-3 boss beam. Also split boss beam rendering into a later top-layer pass from `play.js`, so the active boss beam now sits above the player sprite/combat visuals and the engulfed-by-beam read is preserved.
- 2026-03-21: Tightened the beam-clash presentation again after more feedback. `js/systems/boss.js` now adds a near-parallel clash fallback (not just exact intersection/collinear cases), applies the split-beam rendering path to normal Titan Centipede beams too instead of phase-3-only, and adds a source-over beam veil so the top-layer boss beam covers the player more convincingly. `js/systems/play.js` also moved the boss beam top pass to the end of world rendering so it sits above the full player/world stack.
- 2026-03-21: Tuned `normal-beam` clash geometry separately in `js/systems/boss.js` so phase 1-2 are easier to read during testing. Non-phase-3 clashes now widen the carve corridor much more aggressively by boosting `gapWidth`, `splitOffset`, and split backtrack distance instead of reusing the tighter phase-3 values.
- 2026-03-21: Simplified Titan Centipede clash branch rendering in `js/systems/boss.js` to match the intended shape better. The split-beam branches now use explicit forked branch segments from the centerline toward the clash point via `createTitanCentipedeClashBranchLine()` instead of reusing parallel offset beam segments, so the boss beam can visually split from the impact point rather than looking like two shifted copies.
- 2026-03-21: Added a lighter `simplified clash render` path in `js/systems/boss.js` to reduce stutter while testing. During active beam split/clash, the boss beam now skips the heaviest extra shell/high-graphics/particle passes and keeps a smaller set of core shell/body/seam-glow draws, which should preserve the split read while making the scene much less expensive to render.
- 2026-03-21: Lengthened the visible split branches in `js/systems/boss.js` by increasing `splitBacktrackDistance` again. The forked beam parts now start farther back from the clash point, so the separated boss-beam branches should read longer before merging into the main beam.
- 2026-03-21: Lengthened the visible split branches again in `js/systems/boss.js`, this time directly in render-space. Clash branch lines now start from an earlier `branch start progress` than the state-level split point, so the forked boss-beam arms can read visibly longer even when the core clash state itself remains tight.
- 2026-03-21: Reverted the render-space branch-start extension after it changed the fork shape more than desired. Titan Centipede clash branches are back to the original branch shape, and the latest length tuning now comes only from a much larger `splitBacktrackDistance` window/clamp so the separated beam arms can grow longer without changing their basic form.
- 2026-03-21: Smoothed the clash insertion look in `js/systems/boss.js`. Active clash state now stores player beam direction, split branches use `butt` caps instead of round ends so they stop looking like floating bulbs, and simplified clash rendering adds a short center insertion wedge aimed back along the player beam so the small beam reads as stabbing into the middle of the boss beam rather than hovering beneath it.
- 2026-03-21: Increased split-branch length again in the simplest way: `js/systems/boss.js` now uses much larger `splitBacktrackDistance` multipliers/clamps for both phase-3 and normal beam clashes, without changing the current branch shape logic.
- 2026-03-21: Switched the clash composition to match the simpler intended concept in `js/systems/boss.js`. During a split/clash, the boss beam now keeps drawing its main center line all the way into the clash point, and the separated fork beams are drawn as extra branch overlays on top of that center collision beam instead of replacing the center section.
- 2026-03-21: Tightened the clash silhouette again in `js/systems/boss.js` to better match the intended “beam enters center, outer streams split off” read. The split branches are now parallel side-rails again (not A-shaped fork lines), short connector wedges are drawn at the split start so those rails attach back into the main beam instead of floating, and simplified-clash seam/impact glow are both reduced so the center no longer blooms into a giant blob over the split shape.
- 2026-03-21: Retuned the same Titan Centipede clash silhouette in `js/systems/boss.js` after the side-rails still felt disconnected. The long split rails are still preserved, but their short connector wedges now anchor from the actual impact point instead of the upstream split point, and the simplified clash seam/impact glow were cut down further so the center reads more like a real beam collision with split branches extending away from it.
- 2026-03-21: Temporarily disabled Titan Centipede `beam clash` in `js/systems/boss.js` to reset the feature back to a clean baseline before rebuilding from a new reference. `TITAN_CENTIPEDE_BEAM_CLASH_ENABLED` is now `false`, so clash state no longer updates or participates in beam trim/render logic, while the underlying boss beam behavior remains intact for the next implementation pass.
- 2026-03-21: Rebuilt Titan Centipede `beam clash` from the new simple reference in `js/systems/boss.js`. Clash is enabled again, the boss beam now keeps only a trimmed main lane into the impact point plus two real split branch beams created directly from that impact point, and those split beams now participate in hit checks too instead of being visual-only. I also removed the extra connector/insert/seam-glow layers from the clash path so the shape stays close to the plain three-lane reference.
- 2026-03-21: Corrected the new simple Titan Centipede clash branch direction in `js/systems/boss.js`. The split beam geometry now travels from the boss-side split origin into the impact point instead of being defined from the impact back outward, which keeps the three-lane clash shape the same but fixes the branch beam direction/orientation for rendering and hit behavior.
- 2026-03-21: Corrected the simple Titan Centipede clash orientation again in `js/systems/boss.js` after rechecking the reference. The split beams now extend forward from the impact point into the opposing/player side instead of trailing back toward the boss side, while the trimmed main boss lane still ends at the impact point and the split lanes remain real hit lines.
- 2026-03-21: Simplified Titan Centipede split-beam tuning in `js/systems/boss.js` by replacing the adaptive branch-length formula with a fixed constant. Clash split beams now use `TITAN_CENTIPEDE_BEAM_CLASH_SPLIT_BRANCH_LENGTH_SCALE = 50`, so their forward branch length is a stable `scene.tileSize * 50` before normal beam-range clamping.
- 2026-03-21: Added a persistent yellow spawn-point marker in `js/systems/play.js`, then corrected both its placement and scale. The marker now sits above the start zone and uses the same large trigger-style radius/outline treatment as the blue/red circles instead of the earlier small dot-like marker.
